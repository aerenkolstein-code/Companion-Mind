[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "companion-mind"
version = "0.1.0"
description = "Experimental continuous cognitive runtime skeleton"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["companion_mind*"]
